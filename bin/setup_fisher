#! /usr/bin/env fish
# Install fish plugin
fisher install oh-my-fish/plugin-proxy FabioAntunes/fish-nvm edc/bass

# install node
nvm install lts/fermium && nvm use lts/fermium

# install yarn pnpm
npm install --global yarn pnpm
