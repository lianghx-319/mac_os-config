#! /usr/bin/env bash

# Installs applications.

install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_dmg_app "$CLASHX_APP_NAME" "$CLASHX_VOLUME_NAME" "$CLASHX_APP_URL"
install_zip_app "$NET_NEWS_WIRE_APP_URL" "$NET_NEWS_WIRE_APP_NAME"
